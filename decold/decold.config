enable_migration 1

enable_topk 0

# migrate only files larger than big_file
big_file  10240

# migrate if the file similarity is greater than migration_threshold
migration_threshold  0.8

# rocksdb path
db_path /tmp/db_path

# if db_is_exist=1, we can reuse the db built last time
db_is_exist 1

